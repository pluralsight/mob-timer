{
  "name": "mob-timer",
  "engines": {
    "node": "12.0.0"
  },
  "description": "A cross-platform mob-timer built on Electron for doing Mob Programming.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "build-win": "electron-packager . --platform=win32 --arch=x64 --out=dist --icon=icon --overwrite",
    "package-win": "bestzip dist/mob-timer-win32-x64.zip ./dist/mob-timer-win32-x64",
    "build-mac": "electron-packager . --platform=darwin --arch=x64 --out=dist --icon=icon --overwrite",
    "package-mac": "bestzip dist/mob-timer-darwin-x64.zip ./dist/mob-timer-darwin-x64",
    "build-linux": "electron-packager . --platform=linux --arch=x64 --out=dist --icon=icon --overwrite",
    "package-linux": "bestzip dist/mob-timer-linux-x64.zip ./dist/mob-timer-linux-x64",
    "lint": "eslint --format codeframe .",
    "pretest": "npm run lint",
    "test": "jest .",
    "tdd": "jest --watch",
    "watch": "jest --watch",
    "coverage": "jest --coverage --coverageReporters=text-lcov | coveralls"
  },
  "jest": {
    "modulePathIgnorePatterns": [
      "<rootDir>/dist"
    ],
    "coverageReporters": [
      "text-summary"
    ],
    "collectCoverageFrom": [
      "src/**/*.js",
      "!src/**/*.test.js",
      "!src/**/test-*.js"
    ],
    "collectCoverage": true,
    "resetMocks": true
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mob-timer/mob-timer.git"
  },
  "author": "",
  "license": "Apache-2.0",
  "private": true,
  "bugs": {
    "url": "https://github.com/mob-timer/mob-timer/issues"
  },
  "homepage": "https://mob-timer.github.io",
  "devDependencies": {
    "@babel/core": "^7.0.0",
    "bestzip": "^2.1.4",
    "coveralls": "^3.0.3",
    "electron": "^5.0.0",
    "electron-packager": "^13.0.1",
    "electron-releases": "^3.97.0",
    "eslint": "^6.0.0",
    "eslint-config-prettier": "^6.0.0",
    "eslint-plugin-import": "^2.16.0",
    "eslint-plugin-jest": "^22.6.4",
    "eslint-plugin-node": "^9.0.0",
    "eslint-plugin-prettier": "^3.0.1",
    "eslint-plugin-promise": "^4.0.1",
    "jest": "^24.8.0",
    "prettier": "^1.16.4",
    "sinon": "^7.1.1"
  },
  "dependencies": {
    "debounce": "^1.2.0",
    "pretty-checkbox": "^3.0.3",
    "typeface-work-sans": "0.0.72",
    "uuid": "^3.2.1"
  }
}
